function loadStothramUI(config) {
    // TODO: build UI (title, buttons, textareas, output area) using config
}

function runAllocation(config, options) {
    // TODO: core allocation logic will move here from vsn.js
}
